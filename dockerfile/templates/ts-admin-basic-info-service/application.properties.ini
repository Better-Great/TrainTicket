server.port=${AdminBasicInfoServicePort}
management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true
spring.cloud.nacos.discovery.server-addr=${NacosAddrs}
spring.application.name=ts-admin-basic-info-service
swagger.controllerPackage=adminbasic.controller

contacts-service.url=http://${ContactsServiceHost}:${ContactsServicePort}
station-service.url=http://${StationServiceHost}:${StationServicePort}
train-service.url=http://${TrainServiceHost}:${TrainServicePort}
config-service.url=http://${ConfigServiceHost}:${ConfigServicePort}
price-service.url=http://${PriceServiceHost}:${PriceServicePort}
