databaseChangeLog:
  # ============================================================================
  # Create tables for ts-payment-service
  # ============================================================================
  # Based on com.trainticket.entity.Payment and Money entities:
  # - Default entity name 'Payment' and 'Money'
  # ============================================================================

  - changeSet:
      id: create-payment-table
      author: devops
      comment: Create payment table for ts-payment-service
      changes:
        - createTable:
            tableName: payment
            catalogName: ts-payment-mysql
            columns:
              - column:
                  name: id
                  type: varchar(36)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: order_id
                  type: varchar(36)
                  constraints:
                    nullable: false
              - column:
                  name: user_id
                  type: varchar(36)
                  constraints:
                    nullable: false
              - column:
                  name: payment_price
                  type: varchar(255)
                  constraints:
                    nullable: false

  - changeSet:
      id: create-money-table
      author: devops
      comment: Create money table for ts-payment-service
      changes:
        - createTable:
            tableName: money
            catalogName: ts-payment-mysql
            columns:
              - column:
                  name: id
                  type: varchar(36)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: user_id
                  type: varchar(36)
                  constraints:
                    nullable: true
              - column:
                  name: money
                  type: varchar(255)
                  constraints:
                    nullable: true

