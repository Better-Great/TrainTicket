databaseChangeLog:
  - changeSet:
      id: create-ts-station-database
      author: devops
      comment: Create database for ts-station-service
      changes:
        - sql:
            sql: CREATE DATABASE IF NOT EXISTS `ts-station-mysql` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        - sql:
            sql: GRANT ALL PRIVILEGES ON `ts-station-mysql`.* TO 'beta'@'%';
        - sql:
            sql: FLUSH PRIVILEGES;
