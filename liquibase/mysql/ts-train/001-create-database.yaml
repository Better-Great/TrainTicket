databaseChangeLog:
  - changeSet:
      id: create-ts-train-database
      author: devops
      comment: Create database for ts-train-service
      changes:
        - sql:
            sql: CREATE DATABASE IF NOT EXISTS `ts-train-mysql` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        - sql:
            sql: GRANT ALL PRIVILEGES ON `ts-train-mysql`.* TO 'beta'@'%';
        - sql:
            sql: FLUSH PRIVILEGES;
