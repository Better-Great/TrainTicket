databaseChangeLog:
  - changeSet:
      id: create-ts-station-food-database
      author: devops
      comment: Create database for ts-station-food-service
      changes:
        - sql:
            sql: CREATE DATABASE IF NOT EXISTS `ts-station-food-mysql` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        - sql:
            sql: GRANT ALL PRIVILEGES ON `ts-station-food-mysql`.* TO 'beta'@'%';
        - sql:
            sql: FLUSH PRIVILEGES;
