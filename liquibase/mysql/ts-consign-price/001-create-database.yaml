databaseChangeLog:
  - changeSet:
      id: create-ts-consign-price-database
      author: devops
      comment: Create database for ts-consign-price-service
      changes:
        - sql:
            sql: CREATE DATABASE IF NOT EXISTS `ts-consign-price-mysql` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        - sql:
            sql: GRANT ALL PRIVILEGES ON `ts-consign-price-mysql`.* TO 'beta'@'%';
        - sql:
            sql: FLUSH PRIVILEGES;

