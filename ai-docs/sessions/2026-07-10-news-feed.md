# Session — News feed (TT-219)

**Date:** 2026-07-10  
**Scope:** Whole `TrainTicket/` — SPA + `ts-news-service` tests

## Done

- SPA `/news` with mock + live paths (`/api/v1/newsservice/news`, fallback `/news-service/news`)
- Nav, SEO, sitemap
- Unit tests: mock + services (48 Vitest); `ts-news-service` Go tests (`main_test.go`)

## Verify

```bash
cd ts-ui-web && bun run check
cd ts-news-service && go test ./...
```

## Next

1. Admin Routes CRUD (parity)
2. Admin Trains / Users CRUD
3. Gateway live mode + smoke (deferred)
